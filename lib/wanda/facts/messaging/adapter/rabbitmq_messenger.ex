defmodule Wanda.Facts.Messenger.RabbitMQ do
  @moduledoc """
  RabbitMQ adapter for the Facts Messenger
  """

  @behaviour Wanda.Facts.Messenger.Gen

  def initiate_facts_gathering(execution_id, agent_id, facts) do
  end

  def gather_facts() do
  end
end
